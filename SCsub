#!/usr/bin/env python

# SCons 构建入口：把本模块的所有源文件登记进 Godot 构建系统。

Import("env")
Import("env_modules")

# 克隆一份模块专用环境，避免污染共用环境配置。
env_ext_tools = env_modules.Clone()

# 编译本模块根目录下的所有 .cpp 源文件。
env_ext_tools.add_source_files(env.modules_sources, "*.cpp")